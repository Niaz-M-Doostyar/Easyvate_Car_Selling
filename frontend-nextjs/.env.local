# ============================================================
# SINGLE SWITCH: change to 'vps' when using VPS
#                change to 'local' when running on your machine
# ============================================================
NEXT_PUBLIC_DEPLOY_TARGET=local

# These are set automatically by api.js based on DEPLOY_TARGET:
#   local  →  http://localhost:3001/api
#   vps    →  http://194.163.170.240/api
